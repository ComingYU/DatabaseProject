#include "../catch.hpp"
TEST_CASE("db/block.h test")
{
    SECTION("size")
    {
        REQUIRE(true);
        // Record rec;
        // std::vector<struct iovec> iov;
        // unsigned char header = RECORD_FULL_ALL;
        // unsigned char buffer[1024];
        // iov.push_back({buffer, 1024});
        // rec.set(iov, &header);
        // rec.get(iov, &header);
    }
}